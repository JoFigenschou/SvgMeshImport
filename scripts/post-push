#!/bin/sh
# Auto-sync the Unreal project plugin folder after git push.

REPO_ROOT=$(git rev-parse --show-toplevel)
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$REPO_ROOT/scripts/sync-to-unreal.ps1"
